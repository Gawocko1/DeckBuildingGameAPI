from typing import Literal, List, Tuple

PlayerPosition = int
CardLocation = Literal["drawpile", "hand", "trash", "discardpile", "store"]
CardCategory = Literal["currency", "victory", "action", "other"]